<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Pokemon Database</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #c3e4ff, #e4ffd4);
            min-height: 100vh;
            font-family: 'Segoe UI', sans-serif;
        }

        .container {
            padding-top: 3rem;
            padding-bottom: 3rem;
        }

        .card {
            border-radius: 1rem;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            background-color: #fff;
            padding: 2rem;
        }

        h1 {
            font-weight: 700;
            color: #2c3e50;
        }

        table {
            border-collapse: separate;
            border-spacing: 0 0.5rem;
        }

        .table thead th,
        table thead th {
            background-color: #e9ecef !important;
            color: #2c3e50 !important;
            text-align: center;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .table thead tr:first-child th:first-child {
            border-top-left-radius: 0.5rem;
        }

        .table thead tr:first-child th:last-child {
            border-top-right-radius: 0.5rem;
        }

        td {
            background-color: #f9f9f9;
            text-align: center;
            vertical-align: middle;
        }

        tr:hover td {
            background-color: #eaf6ff;
            transition: background-color 0.2s ease;
        }

        .search-box {
            margin-top: 2rem;
            text-align: center;
        }

        .btn-custom {
            border-radius: 0.5rem;
        }
    </style>

</head>

<body>
    <div class="container">
        <div class="card">
            <h1 class="text-center mb-4">Pokemon Database</h1>

            <div class="search-box">
                <h3 class="mb-3">Search Pokemon</h3>
                <form action="/searchPokemon" method="POST" class="d-flex justify-content-center">
                    <input type="text" name="name" class="form-control w-50 me-2" placeholder="Enter Pokemon name"
                        required>
                    <button type="submit" class="btn btn-primary btn-custom">Search</button>
                </form>
            </div>

            <br><br>

            <% if (error_message) { %>
                <div class="alert alert-danger text-center">
                    <%= error_message %>
                </div>
                <% } %>

                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Name</th>
                                    <th>Base Total</th>
                                </tr>
                            </thead>
                            <tbody>
                                <% pokemonList.forEach(p=> { %>
                                    <tr>
                                        <td>
                                            <%= p.id %>
                                        </td>
                                        <td class="text-capitalize fw-bold">
                                            <%= p.description %>
                                        </td>
                                        <td>
                                            <%= p.base_total %>
                                        </td>
                                    </tr>
                                    <% }) %>
                            </tbody>
                        </table>
                    </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>