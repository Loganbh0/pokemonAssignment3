<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Pokemon Search Result</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #c3e4ff, #e4ffd4);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', sans-serif;
        }

        .card {
            border-radius: 1rem;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
            max-width: 500px;
            width: 100%;
            padding: 2rem;
            text-align: center;
            background-color: #fff;
            transition: transform 0.2s ease, box-shadow 0.2s ease;
        }

        .card:hover {
            transform: scale(1.02);
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.15);
        }

        .pokemon-name {
            font-size: 2rem;
            font-weight: 700;
            color: #2c3e50;
            text-transform: capitalize;
        }

        .pokemon-stat {
            font-size: 1.2rem;
            margin-top: 1rem;
            color: #2c3e50;
        }

        .btn-custom {
            margin-top: 1.5rem;
            border-radius: 0.5rem;
        }
    </style>
</head>

<body>
    <div class="card">
        <% if (pokemon) { %>
            <h1 class="pokemon-name">
                <%= pokemon.description %>
            </h1>
            <p class="pokemon-stat">Base Total: <strong>
                    <%= pokemon.base_total %>
                </strong></p>
            <a href="/" class="btn btn-primary btn-custom">Back to List</a>
            <% } else { %>
                <h2 class="text-danger mb-3">No Pokemon Found</h2>
                <p>Try checking your spelling or searching again.</p>
                <a href="/" class="btn btn-secondary btn-custom">Back to List</a>
                <% } %>
    </div>
</body>

</html>