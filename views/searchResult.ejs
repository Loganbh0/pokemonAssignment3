<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Pokemon Entry</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(145deg, #bae2ff, #005ea2);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Segoe UI', sans-serif;
            color: #2c3e50;
        }

        .pokedex-frame {
            border: 6px solid #333;
            border-radius: 20px;
            background: linear-gradient(180deg, #d32f2f 0%, #b71c1c 100%);
            box-shadow: inset 0 0 15px rgba(255, 255, 255, 0.2), 0 5px 25px rgba(0, 0, 0, 0.3);
            padding: 2rem;
            width: 500px;
            max-width: 90%;
        }

        .led-row {
            display: flex;
            justify-content: center;
            margin-bottom: 1rem;
        }

        .led {
            width: 15px;
            height: 15px;
            border-radius: 50%;
            margin: 0 5px;
        }

        .led.red {
            background: #ff3b30;
            box-shadow: 0 0 10px #ff3b30;
        }

        .led.yellow {
            background: #ffcc00;
            box-shadow: 0 0 10px #ffcc00;
        }

        .led.green {
            background: #4cd964;
            box-shadow: 0 0 10px #4cd964;
        }

        .screen {
            background-color: #f4f4f4;
            border: 3px solid #333;
            border-radius: 12px;
            box-shadow: inset 0 0 10px rgba(0, 0, 0, 0.2);
            padding: 2rem;
            text-align: center;
        }

        h1 {
            font-weight: 700;
            color: #222;
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.2);
            text-transform: capitalize;
        }

        .pokemon-stat {
            font-size: 1.2rem;
            margin-top: 1rem;
            color: #2c3e50;
        }

        .btn-pokedex {
            background-color: #1976d2;
            color: #fff;
            border: none;
            border-radius: 8px;
            padding: 0.6rem 1.2rem;
            font-weight: 600;
            transition: all 0.2s ease;
            margin-top: 1.5rem;
        }

        .btn-pokedex:hover {
            background-color: #0d47a1;
            transform: scale(1.05);
        }

        img {
            width: 150px;
            height: 150px;
            object-fit: contain;
            margin: 1rem auto;
            display: block;
        }
    </style>
</head>

<body>
    <div class="pokedex-frame">
        <div class="led-row">
            <div class="led red"></div>
            <div class="led yellow"></div>
            <div class="led green"></div>
        </div>

        <div class="screen">
            <% if (pokemon) { %>
                <h1>
                    <%= pokemon.description %>
                </h1>
                <!-- optional image from Pokemondb.net -->
                <img src="https://img.pokemondb.net/artwork/<%= pokemon.description.toLowerCase() %>.jpg"
                    alt="<%= pokemon.description %>" onerror="this.style.display='none'">
                <p class="pokemon-stat">Base Total: <strong>
                        <%= pokemon.base_total %>
                    </strong></p>
                <a href="/" class="btn btn-pokedex">Back to List</a>
                <% } else { %>
                    <h2 class="text-danger mb-3">No Pokemon Found</h2>
                    <p>Try checking your spelling or searching again.</p>
                    <a href="/" class="btn btn-secondary btn-pokedex">Back to List</a>
                    <% } %>
        </div>
    </div>
</body>

</html>